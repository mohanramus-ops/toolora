# Toolora

Clean Astro + Netlify CMS starter. Homepage: simple welcome text.

Deploy steps:
1. Upload to GitHub repo root.
2. Connect repo to Netlify (build: `npm install && npm run build`, publish dir: `dist`).
3. Enable Identity & Git Gateway, invite users, visit `/admin` to publish posts.
